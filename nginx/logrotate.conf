# TODO: test log rotation after deploy with a live nginx container.
/var/log/nginx/*.log {
  daily
  rotate 14
  compress
  dateext
  missingok
  notifempty
  sharedscripts
  postrotate
    nginx -s reload >/dev/null 2>&1 || true
  endscript
}
