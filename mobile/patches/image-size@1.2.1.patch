diff --git a/dist/types/icns.js b/dist/types/icns.js
index f2bfafef3723cb423b110304815e56e3f97f81e0..ebab2a4a7a9ca878b18f28e8862bcabceae9d8bd 100644
--- a/dist/types/icns.js
+++ b/dist/types/icns.js
@@ -74,6 +74,16 @@ function getImageSize(type) {
     const size = ICON_TYPE_SIZE[type];
     return { width: size, height: size, type };
 }
+function nextImageOffset(currentOffset, entryLength, fileLength, inputLength) {
+    if (!Number.isSafeInteger(entryLength) || entryLength < SIZE_HEADER) {
+        throw new TypeError('Invalid ICNS entry length');
+    }
+    const nextOffset = currentOffset + entryLength;
+    if (nextOffset <= currentOffset || nextOffset > fileLength || nextOffset > inputLength) {
+        throw new TypeError('Invalid ICNS entry bounds');
+    }
+    return nextOffset;
+}
 exports.ICNS = {
     validate: (input) => (0, utils_1.toUTF8String)(input, 0, 4) === 'icns',
     calculate(input) {
@@ -82,7 +92,7 @@ exports.ICNS = {
         let imageOffset = SIZE_HEADER;
         let imageHeader = readImageHeader(input, imageOffset);
         let imageSize = getImageSize(imageHeader[0]);
-        imageOffset += imageHeader[1];
+        imageOffset = nextImageOffset(imageOffset, imageHeader[1], fileLength, inputLength);
         if (imageOffset === fileLength)
             return imageSize;
         const result = {
@@ -93,7 +103,7 @@ exports.ICNS = {
         while (imageOffset < fileLength && imageOffset < inputLength) {
             imageHeader = readImageHeader(input, imageOffset);
             imageSize = getImageSize(imageHeader[0]);
-            imageOffset += imageHeader[1];
+            imageOffset = nextImageOffset(imageOffset, imageHeader[1], fileLength, inputLength);
             result.images.push(imageSize);
         }
         return result;
diff --git a/dist/types/utils.js b/dist/types/utils.js
index 5224bbafe87551ac415cb3de234820ccc0ff6e2c..1ecaecb85d8a707015a00ede74614f8c32688e80 100644
--- a/dist/types/utils.js
+++ b/dist/types/utils.js
@@ -52,6 +52,8 @@ function readBox(input, offset) {
     if (input.length - offset < 4)
         return;
     const boxSize = (0, exports.readUInt32BE)(input, offset);
+    if (!Number.isSafeInteger(boxSize) || boxSize < 8)
+        return;
     if (input.length - offset < boxSize)
         return;
     return {
